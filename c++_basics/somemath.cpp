  //https://cplusplus.com/reference/cmath/


#include<iostream>
#include <cmath>

int main(){

  double a = 2.34;
  double b = 3.867;
  double z;

  //z = std::max(a,b);
  //z = std::min(a,b);
  //z = pow((int)a,3);
  //z = sqrt(16);
  //z = round(b);
  //z = ceil(a);
  //z = floor(a);

  std::cout << z ;

  return 0;
}